# temp/

Scratch + scrape JSON drops.

Use this folder for:

- Apify scrape outputs (`YYYY-MM-DD_test-100-leads.json`)
- One-off draft files
- Anything you don't yet know where to put

Files older than 30 days get archived. This folder is meant to be messy.
